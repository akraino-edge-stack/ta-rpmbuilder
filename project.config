[access]
	inheritFrom = ta
[access "refs/*"]
	owner = group ldap/akraino-gerrit-ta-rpmbuilder-committers
[project]
	description = the tool used to build the delivery packages of every repository, that is, an RPM
